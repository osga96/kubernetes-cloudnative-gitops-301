apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudnative-demo.fullname" . }}
  labels:
    {{- include "cloudnative-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudnative-demo.selectorLabels" . | nindent 4 }}
